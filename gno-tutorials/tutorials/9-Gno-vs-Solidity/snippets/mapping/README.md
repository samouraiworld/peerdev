# Mapping

Key-value storage and retrieval patterns.

**Key Difference:** Solidity has native `mapping` type, Gno uses `avl.Tree` for key-value storage.

## Testing

Run from the tutorial root directory:
```bash
# Test Gno version
./test-snippets.sh 6 gno

# Test Solidity version
./test-snippets.sh 6 sol
```
