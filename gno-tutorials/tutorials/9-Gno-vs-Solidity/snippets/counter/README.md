# Counter

Basic counter implementation showing state management and incrementing values.

**Key Difference:** Solidity uses `uint256` with public state variables, Gno uses `int` with getter functions.
